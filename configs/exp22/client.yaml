# exp22_fedprox_radimagenet — NODOS CLIENTE (5 nodos, FedProx, RadImageNet init)
#
# Mapeo nodo → dataset (igual que exp07/exp12/exp13/exp14/exp19):
#   node1 → cmmd         (5202 imgs: 1108 benign / 4094 malignant)
#   node2 → inbreast     ( 410 imgs: 310 benign / 100 malignant)
#   node3 → cdd-cesm     (1003 imgs: 672 benign / 331 malignant)
#   node4 → kau-bcmd     (2206 imgs: 2112 benign /  94 malignant)
#   node5 → dmid         ( 510 imgs: 366 benign / 144 malignant)
#
# Cada nodo usa ESTE mismo YAML. Lo específico de cada nodo se pasa por CLI:
#   --manifest manifests/node<ID>_manifest.csv
#
# CONFIGURACIÓN:
#   - weight_source: radimagenet (RadImageNet directo, sin pretrain DDSM)
#   - freeze_backbone: false (fine-tuning completo)
#   - Sin descongelamiento progresivo
#   - FedProx con proximal_mu: 0.1 para regularización en no-IID
#   - 20 rondas, 5 épocas locales (igual a exp21 FedAvg para comparabilidad)
#
# COHERENCIA servidor↔cliente:
#   model.freeze_backbone, local_unfreeze_at_epoch, unfreeze_layers,
#   training.scheduler.t_max, federated.rounds y federated.strategy DEBEN coincidir con server.yaml.

name: exp22_fedprox_radimagenet
mode: federated
seed: 42
output_dir: runs/exp22_fedprox_radimagenet
device: auto

data:
  name: mammo_bench
  manifest_path: manifests/node0_manifest.csv   # sobreescrito por --manifest en el workflow
  image_root: data/
  image_size: 224
  grayscale: false
  num_classes: 2
  batch_size: 32
  num_workers: 0
  val_fraction: 0.15      # IGNORADO: el manifest ya trae split explícito
  test_fraction: 0.0      # IGNORADO: idem
  image_format: jpg
  normal_policy: benign
  balance_classes: true

model:
  name: resnet50
  weight_source: radimagenet                     # RadImageNet directamente
  checkpoint_path: weights/RadImageNet-resnet50.pth
  strict_load: false
  in_channels: 3
  num_classes: 1
  dropout: 0.0
  freeze_backbone: false                         # fine-tuning completo
  unfreeze_at_epoch: null
  local_unfreeze_at_epoch: null                  # sin descongelamiento progresivo
  unfreeze_layers: null

training:
  local_epochs: 5                 # 5 épocas locales por ronda — DEBE coincidir con el servidor
  grad_clip_norm: 1.0
  mixed_precision: true
  optimizer:
    name: adamw
    lr: 1.0e-4
    lr_head: 1.0e-3
    lr_backbone: 1.0e-4
    weight_decay: 1.0e-4
  scheduler:
    name: cosine
    t_max: 5                      # = local_epochs (el scheduler se reinicia cada ronda)
  augmentation:
    horizontal_flip: true
    rotate_limit: 15
    brightness_contrast: true
    normalize_preset: radimagenet_rgb
  loss:
    name: bce
    auto_class_weights: true

evaluation:
  threshold: 0.5
  save_predictions: true

federated:
  rounds: 20                       # DEBE coincidir con el servidor
  strategy:
    name: fedprox
    params:
      proximal_mu: 0.1             # DEBE coincidir con el servidor
