# Runtime requirements for src/ on feature/radimagenet-refactor.
#
# Narrower on purpose than feature/radimagenet's requirements.txt: this
# branch's current scope is centralized training only (see REFACTOR.md).
# flwr/ray (federated), opencv/albumentations/pydicom/scikit-learn
# (unused so far -- src/ transforms go through torchvision only), wandb
# (stays notebook-only, see CLAUDE.md), matplotlib/tqdm (no plotting in
# src/ yet) are all deliberately left out. Add them back only once
# something under src/ actually imports them, not preemptively.
#
# For CUDA wheels, install torch + torchvision separately from
# https://download.pytorch.org/whl/<cuXXX> BEFORE installing the rest:
#   pip install --index-url https://download.pytorch.org/whl/cu121 torch torchvision
#   pip install -r requirements.txt
# CPU-only is the default if you `pip install -r requirements.txt` directly.

# --- Deep learning ---
torch>=2.13,<3.0
torchvision>=0.28,<1.0
torchmetrics>=1.9,<2.0

# --- Data + numerics ---
numpy>=2.5,<3.0
pandas>=3.0,<4.0
pillow>=12.0,<13.0

# --- Config / logging ---
pydantic>=2.13,<3.0
PyYAML>=6.0,<7.0
tensorboard>=2.21,<3.0

# Optional -- only imported at runtime if train.wandb_project is set in the
# experiment YAML (see tracking.py's lazy `import wandb`). Safe to omit if
# you never use it; nothing else in src/ depends on it.
wandb>=0.29,<0.30
