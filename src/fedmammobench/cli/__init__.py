"""Installable CLI entry points.

These are thin re-exports of the ``scripts/*.py`` files, used by the
``[project.scripts]`` table in ``pyproject.toml`` so users get
``fedmammobench-centralized``, ``fedmammobench-federated``, and ``fedmammobench-evaluate``
commands after ``pip install``.
"""
